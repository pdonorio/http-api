
rest:
  environment:
    APP_MODE: debug
  ports:
    - 8080:5000
  links:
      # Graphdb must be called gdb
      - graphdb:gdb
      # Redis db (as a queue) should be called queue
      - redqueue:queue
      # # Elasticsearch must be called el
      # - searchindex:el

worker:
  links:
      - redqueue:queue

graphdb:
  ports:
    - 9090:7474

apitests:
  links:
    - rest:apiserver
