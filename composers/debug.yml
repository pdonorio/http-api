
rest:
  environment:
    APP_MODE: debug
  ports:
    - 8080:5000
  links:
      # GRAPHDB MUST BE CALLED gdb
      - graphdb:gdb

graphdb:
  ports:
    - 9090:7474

apitests:
  links:
    - rest:apiserver
