
rest:
  environment:
    APP_MODE: debug
  ports:
    - 8080:5000
  links:
      # Graphdb must be called gdb
      - graphdb:gdb
      # Elasticsearch must be called el
      - searchindex:el

graphdb:
  ports:
    - 9090:7474

apitests:
  links:
    - rest:apiserver
